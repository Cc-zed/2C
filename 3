#include <iostream>
#include <iomanip>

using namespace std;


/*-----------------------Task 1-----------------------

int main() {
    double distance, time;

    cout << "Wnter vidstan to airoport (y km): ";
    cin >> distance;

    cout << "Enter time doizdy (y hours): ";
    cin >> time;

    double speed = distance / time;

    cout << "Speed you need to move with: " << speed << " km/hour" << endl;

    return 0;
}*/


/*-----------------------Task 2-----------------------

int main() {
    int start_hour, start_minute, start_second;
    int end_hour, end_minute, end_second;


    int total_minutes;
    double cost_per_minute = 2.0;
    double total_cost;

    cout << "Enter starts time (Hours minutes seconds): ";
    cin >> start_hour >> start_minute >> start_second;

    cout << "Enter finish time (Hours minutes seconds): ";
    cin >> end_hour >> end_minute >> end_second;

    total_minutes = (end_hour - start_hour) * 60 + (end_minute - start_minute) + (end_second - start_second) / 60;

    total_cost = total_minutes * cost_per_minute;

    cout << "All time: " << total_minutes << " minutes" << endl;
    cout << "Sum: " << total_cost << " hryvnas" << endl;

    return 0;
}*/


/*-----------------------Task 2-----------------------

int main() {
    double distance, fuel_consumption;
    double cost_1, cost_2, cost_3;

    cout << "Enter distance (y km): ";
    cin >> distance;

    cout << "enter waste om 100 km (y litrax): ";
    cin >> fuel_consumption;

    cout << "Enter cost of first type of palne (hrns/litr): ";
    cin >> cost_1;

    cout << "Enter cost of second type of palne (hrns/litr): ";
    cin >> cost_2;

    cout << "Enter cost of third type of palne (hrns/litr): ";
    cin >> cost_3;

    double total_cost_1 = (distance / 100) * fuel_consumption * cost_1;
    double total_cost_2 = (distance / 100) * fuel_consumption * cost_2;
    double total_cost_3 = (distance / 100) * fuel_consumption * cost_3;

    cout << setw(15) << "Type of palne " << setw(15) << "   Price of podoroz" << endl;
    cout << setw(15) << "Palne 1" << setw(15) << total_cost_1 << " hrn" << endl;
    cout << setw(15) << "Palne 2" << setw(15) << total_cost_2 << " hrn" << endl;
    cout << setw(15) << "Palne 3" << setw(15) << total_cost_3 << " hrn" << endl;

    return 0;
}*/

//----------------------- END -----------------------
